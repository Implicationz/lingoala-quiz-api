spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
cors:
  allowed-origins:
    - http://localhost:5173
    - capacitor://localhost
    - http://localhost
jwt:
  secret: ${JWT_SECRET:testsecret}